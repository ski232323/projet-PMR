version: "3.8"

services:
  web:
    image: nginx:alpine
    container_name: navigation-app
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - app-network
    deploy:
      resources:
        limits:
          memory: 256M

  jupyter:
    image: jupyter/minimal-notebook
    container_name: jupyter_lab_PMR
    user: root
    restart: unless-stopped
    volumes:
      - ./app:/home/jovyan/work
    ports:
      - "8899:8888"
    environment:
      - JUPYTER_TOKEN=${JUPYTER_TOKEN}  # Utilise une variable d'environnement
    command: >
      start-notebook.sh
      --NotebookApp.token='${JUPYTER_TOKEN}'
      --NotebookApp.password=''
      --NotebookApp.allow_root=True
      --ip=0.0.0.0
    networks:
      - app-network
    deploy:
      resources:
        limits:
          memory: 512M

networks:
  app-network:
    driver: bridge